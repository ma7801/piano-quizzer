import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pianoquizzer.app',
  appName: 'Piano Quizzer',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
