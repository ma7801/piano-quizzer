import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Piano Quizzer',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
